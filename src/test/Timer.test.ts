// // import Timer from "../src/Timer"
// jest.mock("../src/Timer")

// it('Podemos verificar que el consumidor llamó al constructor de la clase', () => {
//   const timer = new Timer("30:20");
//   expect(timer).toHaveBeenCalledTimes(1);
// });